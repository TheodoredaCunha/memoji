# Kanji Flashcards

## Translation backend

Start the app with:

```bash
npm install
npm run dev
```

This launches:
- the Vite frontend
- the translation backend on port 3001

Set the backend environment variable:

```env
GOOGLE_TRANSLATE_API_KEY=your_google_cloud_translate_api_key
```
